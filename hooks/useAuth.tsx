// 'use client';
// import { AuthContext, AuthContextType } from '@/providers/AuthProvider';
// import { useContext } from 'react';

// const useAuth = () => {
//   const { authLoading, loginUser, logOutUser, registerUser, updateUser, user } =
//     useContext(AuthContext) as AuthContextType;
//   return { authLoading, loginUser, logOutUser, registerUser, updateUser, user };
// };

// export default useAuth;
